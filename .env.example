# This file is NOT loaded by the Flutter app automatically.
# Use one of:
#   flutter run --dart-define=OPENAI_API_KEY=sk-your-key-here
#   flutter run --dart-define-from-file=api-keys.json
# (JSON: {"OPENAI_API_KEY":"sk-..."})
#
# Or enter the key in-app; it is stored with flutter_secure_storage.

OPENAI_API_KEY=
